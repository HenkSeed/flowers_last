$(document).ready(function () {
	flowersSliderFunc(); // Функция обработки слайдера цветов (в файле flowersSliderFunc)
	reviewSliderFunc(); // Функция обработки слайдера отзывов (в файле reviewSliderFunc)
});
